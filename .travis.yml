language: cpp
compiler: gcc
dist: trusty
sudo: required

install:
  # Get the linter
  - sudo apt-get install uncrustify

script:
  - make -j$(nproc)
  - uncrustify --version
  - test/lint/run-linter.bash
  - cd test/scanner
  - ./testall.sh
