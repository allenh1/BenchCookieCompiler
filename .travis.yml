language: cpp
compiler: gcc
dist: trusty
sudo: required

install:
  # Get the linter
  - sudo apt-get install uncrustify

script:
  - make -j$(nproc)
  - test/lint/run-linter.bash
  - cat src/**/*.patch
  - cat include/**/*.patch
  - cd test/scanner
  - ./testall.sh
